spring:
  application:
    name: core-api
  data:
    mongodb:
      uri: mongodb://localhost:27017/hikmetcakirblog_core
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379

server:
  port: 8080